YOLO detection results streamed from Python to Unity using TCP sockets.

Features

- YOLO inference (Ultralytics)
- JSON serialization
- TCP Socket communication
- Unity Prefab instantiation
- ID-based object tracking

##

git fetch origin
git checkout master
git reset --hard origin/master
git clean -fd
